
function main(out) {
	'use strict';
	
	var utilities = require('utilities');
	var http = require("http");
	
	// var sendToPort = out.sendToPort = (content, type, port, host, url) => {
	// 	http.createServer((request, respone) => {
	// 		respone.writeHead(200, {
	// 			"Content-Type": type,
	// 			"Host": host
	// 		});
	// 		respone.end(content);
	// 	}).listen(port, url);
	// };
	
	
	
}

main(exports);